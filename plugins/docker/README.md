# docker

docker: container/image overview, shell-in, prune, compose

**Group:** dev  
**State:** on  
**Requires:** `docker`  

## Enable

```sh
conf on docker && conf reload
```

## Provides

| kind | name | usage | description |
|---|---|---|---|
| func | `dcd` | `dcd [args]` | compose down |
| func | `dcl` | `dcl [service]` | follow compose logs |
| func | `dclean` | - | prune stopped containers and dangling images (shows usage, asks first) |
| func | `dcps` | - | compose service status |
| func | `dcr` | `dcr [service]` | restart compose services |
| func | `dcu` | `dcu [service]` | compose up, detached |
| func | `dimg` | - | images with sizes and age |
| func | `dip` | `dip <container>` | a container's IP address(es) |
| func | `dlog` | `dlog <container>` | follow a container's logs (last 100 lines first) |
| func | `dps` | `dps [docker ps args]` | running containers: name, status, ports, image |
| func | `dpsa` | - | every container including stopped ones |
| func | `dsh` | `dsh <container>` | interactive shell inside a container (bash, falling back to sh) |
| func | `dstopall` | - | stop every running container (asks first) |

---
Generated by `forge doc docker`.
