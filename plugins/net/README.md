# net

net: ports, local/public IP, DNS, HTTP probing, port waiting

**Group:** net  
**State:** on  
**Requires:** `ss curl dig`  

## Enable

```sh
conf on net && conf reload
```

## Provides

| kind | name | usage | description |
|---|---|---|---|
| func | `dns` | `dns <hostname>` | A/AAAA/CNAME/MX/TXT/NS records for a name |
| func | `hdr` | `hdr <url>` | HTTP response headers, following redirects |
| func | `httptime` | `httptime <url>` | break an HTTP request into dns/tcp/tls/ttfb timings |
| func | `localip` | - | this machine's LAN addresses per interface |
| func | `port` | `port <number>` | what is listening on one port |
| func | `ports` | `ports [pattern]` | listening TCP/UDP sockets, optionally filtered |
| func | `pubip` | - | your public IP (contacts ifconfig.me; never called automatically) |
| func | `scan` | `scan <host> [from] [to]` | sweep a TCP port range using /dev/tcp |
| func | `waitport` | `waitport <host> <port> [timeout]` | block until a TCP port accepts connections |

---
Generated by `forge doc net`.
